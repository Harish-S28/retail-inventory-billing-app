# Local dev: leave unset, Vite proxies /api to localhost:4000 automatically.
# Production (Vercel): set this to your deployed backend URL + /api,
# e.g. VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
